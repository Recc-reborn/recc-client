package com.recc.recc_client.layout.recyclerview.adapters

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class ArtistGridAdapter(private val view: View): RecyclerView.ViewHolder(view) {
    private lateinit var binding:

    fun bind() {

    }
}